# PyFlare Docker Environment Configuration (DEVELOPMENT)
# =======================================================
#
# WARNING: This configuration is for LOCAL DEVELOPMENT ONLY.
# For production, use .env.prod.example with docker-compose.prod.yml
#
# SECURITY: Copy this file to .env and configure your credentials.
# NEVER commit .env files to version control.
#
# Development usage:
#   cp .env.example .env
#   # Edit .env with your credentials
#   docker-compose up -d
#
# Production usage (recommended):
#   cp .env.prod.example .env.prod
#   # Edit .env.prod with secure credentials
#   docker-compose -f docker-compose.prod.yml --env-file .env.prod up -d

# =============================================================================
# Required Credentials (must be set)
# =============================================================================

# ClickHouse database password (REQUIRED)
# Generate a secure password: openssl rand -base64 32
CLICKHOUSE_PASSWORD=

# =============================================================================
# Optional Credentials (recommended for production)
# =============================================================================

# Redis password (optional, but recommended for production)
# Generate a secure password: openssl rand -base64 32
REDIS_PASSWORD=

# Qdrant API key (optional, but recommended for production)
# Generate a secure key: openssl rand -hex 32
QDRANT_API_KEY=

# =============================================================================
# Service Configuration
# =============================================================================

# PyFlare log level (trace, debug, info, warn, error)
PYFLARE_LOG_LEVEL=info

# ClickHouse database name and user
CLICKHOUSE_DB=pyflare
CLICKHOUSE_USER=pyflare

# Kafka cluster ID (change for production)
KAFKA_CLUSTER_ID=pyflare-local-cluster

# =============================================================================
# TLS Configuration (optional, recommended for production)
# =============================================================================

# Enable TLS for collector endpoints
PYFLARE_TLS_ENABLED=false

# Path to TLS certificate (inside container)
PYFLARE_TLS_CERT_PATH=

# Path to TLS private key (inside container)
PYFLARE_TLS_KEY_PATH=

# =============================================================================
# Production Recommendations
# =============================================================================
# 1. Use strong, unique passwords for all services
# 2. Enable TLS for all external endpoints
# 3. Use Docker secrets for sensitive values in Swarm mode
# 4. Restrict network access with firewall rules
# 5. Enable Kafka SASL authentication for production
# 6. Use separate credentials for each environment (dev/staging/prod)
